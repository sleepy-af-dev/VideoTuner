## Summary

<!-- What does this change, and why? -->

## Checklist

- [ ] Tests pass (`uv run pytest`)
- [ ] Lint and types pass (`uv run ruff check`, `uv run basedpyright src tests`)
- [ ] Updated the README and release notes (`src/videotuner/version.py`) if behaviour or config changed
